<h3>Post a Comment</h3>

<%= form_for [ @trip, @comment ] do |f| %>
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit', remote: true %>
  </p>
<% end %>
